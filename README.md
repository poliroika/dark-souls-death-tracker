# ⚔️ Dark Souls Remastered Death Tracker

Счетчик смертей для Dark Souls Remastered с прозрачным оверлеем в реальном времени.

## 🎮 Описание

Приложение читает количество смертей напрямую из памяти процесса игры и отображает счетчик прозрачным текстом поверх экрана.

## 📋 Требования

- Windows 10/11
- Python 3.8+
- Dark Souls Remastered (Steam)
- **Запуск от администратора** (для чтения памяти)

## 🚀 Быстрый старт

### 1. Установка

```powershell
uv sync
```

### 2. Запуск (от администратора!)

```powershell
uv run python main.py
```

### 3. Запустите Dark Souls Remastered

Оверлей автоматически подключится к игре.

## 🎛️ Параметры

| Параметр | Описание |
|----------|----------|
| `--minimal` или `-m` | Упрощённый оверлей |
| `--check` или `-c` | Проверить зависимости |

## 🖱️ Управление оверлеем

- **Левая кнопка мыши** — перетаскивание
- **Правая кнопка мыши** — меню / закрыть
- **Ctrl+C** в терминале — выход

## ⚙️ Настройка

Отредактируйте `config.py`:

```python
OVERLAY_FONT = "Arial"           # Шрифт
OVERLAY_FONT_SIZE = 28           # Размер
OVERLAY_TEXT_COLOR = "#FFFFFF"   # Цвет текста
OVERLAY_OPACITY = 0.85           # Прозрачность

OVERLAY_POSITION_X = 50          # Позиция X
OVERLAY_POSITION_Y = 50          # Позиция Y

DEATH_TEXT_TEMPLATE = "Смертей: {count}"  # Формат текста
```

## 🔧 Проблемы

### "Процесс не найден"
- Запустите Dark Souls Remastered
- Убедитесь что скрипт запущен **от администратора**

### "Не удалось найти GameDataMan"
Pointer offset может отличаться для вашей версии игры. Попробуйте найти актуальный offset через Cheat Engine.

### Оверлей не видно поверх игры
Запустите игру в **оконном** или **borderless windowed** режиме.

## 📁 Файлы

```
dark-souls-death-tracker/
├── main.py           # Точка входа
├── memory_reader.py  # Чтение памяти процесса
├── overlay.py        # Прозрачный оверлей
├── config.py         # Настройки
├── pyproject.toml    # Зависимости
└── README.md
```

## 🔒 Безопасность

- Только **читает** память, не модифицирует
- Не является читом
- Безопасно для VAC/EAC

---

**Удачи, Chosen Undead!** 🔥
